"use client";

/**
 * useOfflineListener — DEPRECATED.
 * The application is now online-only (API-first). Offline queue and
 * cross-tab sync have been removed. This hook is kept as a no-op so
 * any stale imports don't break the build.
 */
export function useOfflineListener() {
  // no-op — online-only architecture
}
